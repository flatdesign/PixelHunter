const LEVELS = 3;
const LIVES = 3;
const TIME = 30;


const answerType = {
  wrong: `wrong`,
  slow: `slow`,
  fast: `fast`,
  correct: `correct`,
  unknown: `unknown`
};

export default {LEVELS, LIVES, TIME, answerType};
